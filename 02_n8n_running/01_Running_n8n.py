'''
1. Open your browser
   - Go to: https://yourdomain.com
   WHY: This is where your n8n is hosted and accessible.

2. Login to n8n
   - Enter username & password (set during setup)
   WHY: Protects your workflows and data from others.

3. Create your first workflow
   - Click "New Workflow"
   WHY: Workflows are where automation happens.

4. Add a Trigger node
   - Example: Webhook / Cron / Manual Trigger
   WHY: Every workflow needs a starting point (what triggers it).

5. Add Action nodes
   - Example: HTTP Request, Gmail, Telegram, OpenAI, etc.
   WHY: These perform the actual tasks (send message, fetch data, etc.)

6. Connect nodes
   - Drag from one node to another
   WHY: Defines the flow (what happens after what).

7. Configure each node
   - Fill API keys, inputs, settings
   WHY: Without configuration, nodes don’t work.

8. Test the workflow
   - Click "Execute Workflow"
   WHY: Check if everything works before going live.

9. Fix errors (if any)
   - Check logs/output of each node
   WHY: Debugging ensures correct automation.

10. Activate the workflow
    - Toggle "Active"
    WHY: Makes it run automatically (without manual trigger).

11. Monitor runs
    - Check executions tab
    WHY: See what’s working and what’s failing.

12. Start building real use cases
    - Examples:
      • Auto-send WhatsApp alerts
      • AI chatbot
      • Email automation
      • Web scraping
    WHY: This is where real value comes from 🚀
'''